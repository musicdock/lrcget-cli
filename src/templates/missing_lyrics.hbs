<!DOCTYPE html>
<html>
<head>
    <title>Missing Lyrics Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .header { background: #f5f5f5; padding: 20px; border-radius: 8px; }
        .stats { display: flex; gap: 20px; margin: 20px 0; }
        .stat { background: #e3f2fd; padding: 15px; border-radius: 5px; text-align: center; }
        .track { margin: 10px 0; padding: 10px; border-left: 4px solid #ff5722; background: #fafafa; }
        .track-title { font-weight: bold; color: #d32f2f; }
        .track-info { color: #666; font-size: 0.9em; }
    </style>
</head>
<body>
    <div class="header">
        <h1>🎵 Missing Lyrics Report</h1>
        <p>Generated: {{format_date timestamp "%B %d, %Y at %H:%M UTC"}}</p>
        <p>LRCGET v{{version}}</p>
    </div>

    <div class="stats">
        <div class="stat">
            <h3>{{stats.total_tracks}}</h3>
            <p>Total Tracks</p>
        </div>
        <div class="stat">
            <h3>{{stats.tracks_missing_lyrics}}</h3>
            <p>Missing Lyrics</p>
        </div>
        <div class="stat">
            <h3>{{format_percentage stats.coverage_percentage 1}}</h3>
            <p>Coverage</p>
        </div>
    </div>

    <h2>Tracks Missing Lyrics</h2>
    {{#each tracks}}
    {{#unless lrc_lyrics}}
    {{#unless txt_lyrics}}
    <div class="track">
        <div class="track-title">{{artist_name}} - {{title}}</div>
        <div class="track-info">
            Album: {{album_name}} | 
            Duration: {{format_duration duration}} |
            File: {{truncate file_path 80}}
        </div>
    </div>
    {{/unless}}
    {{/unless}}
    {{/each}}

    <div style="margin-top: 40px; padding: 20px; background: #e8f5e8; border-radius: 8px;">
        <h3>💡 Recommended Actions</h3>
        <ul>
            <li>Run: <code>lrcget download --missing-lyrics</code></li>
            <li>For specific artists: <code>lrcget download --artist "Artist Name"</code></li>
            <li>Check cache stats: <code>lrcget cache stats</code></li>
        </ul>
    </div>
</body>
</html>