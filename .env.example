# LRCGET CLI Environment Configuration
# Copy this file to .env and modify as needed

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Path to the main application database
LRCGET_DATABASE_PATH=/data/lrcget.db

# Optional: Path to local LRCLIB database for faster searches
# Download from: https://lrclib.net/dump (if available)
LRCGET_LRCLIB_DATABASE_PATH=/data/lrclib.db

# ===========================================
# LRCLIB API CONFIGURATION  
# ===========================================
# LRCLIB instance URL (default: https://lrclib.net)
LRCGET_LRCLIB_INSTANCE=https://lrclib.net

# ===========================================
# BEHAVIOR CONFIGURATION
# ===========================================
# Skip tracks that already have synced lyrics (default: true)
LRCGET_SKIP_TRACKS_WITH_SYNCED_LYRICS=true

# Skip tracks that already have plain lyrics (default: false)
LRCGET_SKIP_TRACKS_WITH_PLAIN_LYRICS=false

# Try to embed lyrics into audio files (default: false)
# Note: Requires write access to music files
LRCGET_TRY_EMBED_LYRICS=false

# Show line count in lyrics output (default: true)
LRCGET_SHOW_LINE_COUNT=true

# ===========================================
# WATCH COMMAND CONFIGURATION
# ===========================================
# Default debounce time for watch command in seconds (default: 10)
LRCGET_WATCH_DEBOUNCE_SECONDS=10

# Default batch size for watch command (default: 50)
LRCGET_WATCH_BATCH_SIZE=50

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
# Rust logging level (error, warn, info, debug, trace)
RUST_LOG=debug

# ===========================================
# CACHE CONFIGURATION
# ===========================================
# Optional: Redis URL for hybrid cache (Redis + File cache)
# When Redis is available, enables ultra-fast shared cache
# Falls back to file cache if Redis is unavailable
# LRCGET_REDIS_URL=redis://localhost:6379

# Redis with authentication
# LRCGET_REDIS_URL=redis://:password@localhost:6379

# Redis with custom database
# LRCGET_REDIS_URL=redis://localhost:6379/1

# Redis over SSL/TLS
# LRCGET_REDIS_URL=rediss://localhost:6380

# ===========================================
# DOCKER CONFIGURATION
# ===========================================
# Indicates running in Docker (automatically set)
DOCKER=1